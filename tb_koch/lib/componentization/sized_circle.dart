import 'package:flutter/material.dart';
/*
class SizedCircle extends StatelessWidget {
  const SizedCircle({Key key, @required this.child, this.elevation=0}): super(key: key);

  final Widget child;
  final double elevation;

  @override
  Widget build(BuildContext context){
    return Material(
      child: child,
      shape: CircleBorder(),
      elevation: elevation,
    );
  }
}

*/